{
  "manifest_version": 3,
  "name": "Hide Elements by ID",
  "version": "1.1",
  "description": "Hide parent elements for specific child IDs (e.g. Swagger operations).",
  "permissions": ["storage", "tabs"],
  "action": {
    "default_popup": "popup.html",
    "default_title": "Hide by ID"
  },
  "content_scripts": [
    {
      "matches": [
        "<all_urls>"
      ],
      "js": ["content.js"],
      "run_at": "document_idle"
    }
  ]
}
